# 题库答案核查说明

核查日期：2026-07-12

## 最终采用规则

- 所有题型严格采用两份 PDF 中每题显示的绿色“正确答案”。
- 不采用“我的答案”，也不根据技术知识自行修改 PDF 答案。
- 第 1 道选择题按新版 PDF 的“正确答案”设为 B。

## 核查结果

- 网站共 217 题：填空 24 题、简答 15 题、单选 105 题、多选 38 题、判断 35 题。
- 《选择+判断.pdf》中 178 道题的“正确答案”均已读取，没有缺题。
- 网站判题答案已逐题覆盖为“正确答案”。

## 填空和简答

1. 填空第 2 题：`uni-icons` 没有 `rotate` 属性，应通过 `style` 中的 `transform: rotate(...)` 设置旋转。
2. 填空第 9 题：Promise 三种状态为 `pending`、`fulfilled`、`rejected`，`resolved` 不是三种状态之一。
3. 简答第 31 题：补充说明 HTTP 404 通常仍会进入 `uni.request` 的 `success` 回调，需要检查 `statusCode`。
4. 简答第 33 题：Postman 各阶段耗时通过悬停响应时间查看，Console 主要用于请求和响应日志。
《简答+填空.pdf》实际包含 10 页，24 道填空题和 15 道简答题均显示绿色“正确答案”。网站已逐题采用该栏内容。

- 填空第 2 题采用：`color`、`rotate`。
- 填空第 4 题采用：`xs`、`xl`。
- 填空第 13 题采用：`header`、`{ }`。
- 所有简答题参考答案均采用 PDF 中的“正确答案”原文。
